const double = (array) => {
  let newArr = [];
  for (let i = 0; i < array.length; i++) {
    newArr.push(2 * arr[1]);
  }
  return newArr;
};

// O(n) 1 assignment, but new array grows according to n
